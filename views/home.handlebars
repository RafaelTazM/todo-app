<h2>Home</h2>
<main>
    <section class="tarefas">
         {{#each tarefas}}
        <div class="tarefa">
            <form action="">

                    {{#if this.completa }}
                <div class="completa">
                    <input type="checkbox" id="tarefa-{{this.id}}" checked>
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>
                {{else}}

                    <input type="checkbox" id="tarefa-{{this.id}}" onchange="completarTarefa({{this.id}})">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>
                            {{/if}}

                <button>
                    <img src="/imagens/x-icon.png" alt="Icone de x para exluir tarefa">
                </button>
            </form>
        </div>
          <div class="tarefa">
            <form action="">
                <div>
                    <input type="radio" id="tarefa-1">
                    <label for="tarefa-1">Completar curso de JavaScript</label>
                </div>
                <button>
                    <img src="/imagens/x-icon.png" alt="Icone de x para exluir tarefa">
                </button>
            </form>
        </div>
        <div class="tarefa">
            <form action="">
                <div>
                    <input type="radio" id="tarefa-1">
                    <label for="tarefa-1">Completar curso de JavaScript</label>
                </div>
                <button>
                    <img src="/imagens/x-icon.png" alt="Icone de x para exluir tarefa">
                </button>
            </form>
        </div>
                {{/each}}

        <div class="informacoes">
            <p>5 tarefas ativas</p>
            <div class="botoes">
                <a href="" class="active">Todas</a>
                <a href="">Ativas</a>
                <a href="">Completas</a>        
            </div>

            <a href="">Limpar tarefas</a>
        </div>
    </section>
     <section class="botoes">
        <a href="" class="active">Todas</a>
        <a href="">Ativas</a>
        <a href="">Completas</a>
    </section>
</main>